# Environment / secrets
.env

# Python
venv/
__pycache__/
*.pyc

# SQLite files used only for local testing/verification, never real storage
test_verify.db

# Node / frontend
node_modules/
dist/

# Review service storage (generated at runtime, not source)
review-service/reviews/*
!review-service/reviews/.gitkeep

# Document service storage (generated at runtime, not source)
document-service/data/files/*
document-service/data/meta/*
!document-service/data/files/.gitkeep
!document-service/data/meta/.gitkeep

# OS
.DS_Store

# Claude Code local settings
.claude/
